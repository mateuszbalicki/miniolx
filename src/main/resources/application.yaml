spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miniolx_db
    username: miniolx_user
    password: supertajnehaslo
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update